<div class="page-header page-header--actions">
  <div>
    <h1 class="page-header__title">Projects</h1>
  </div>
  <a href="/projects/new" class="button button--primary">New Project</a>
</div>

<% if (projects.length === 0) { %>
<div class="empty-state">
  <div class="empty-state__icon"><%- include('../../partials/icon', { name: 'projects' }) %></div>
  <h2 class="empty-state__title">No projects yet</h2>
  <p class="empty-state__description">Create a project, connect a repository, then prepare it for deployment.</p>
  <ul class="empty-state__steps">
    <li>Create the project shell.</li>
    <li>Connect GitHub and choose the production branch.</li>
    <li>Run detection, configure secrets, then deploy.</li>
  </ul>
  <div class="empty-state__actions">
    <a href="/projects/new" class="button button--primary">Create Project</a>
  </div>
</div>
<% } else { %>
<div class="table-wrapper table-responsive">
  <table class="table table--responsive">
    <thead>
      <tr>
        <th scope="col">Name</th>
        <th scope="col">Status</th>
        <th scope="col">Role</th>
        <th scope="col">Created</th>
        <th scope="col"><span class="sr-only">Actions</span></th>
      </tr>
    </thead>
    <tbody>
      <% projects.forEach(({ project, role }) => { %>
      <tr>
        <td data-label="Name">
          <a href="/projects/<%= project.slug %>" class="table-link"><strong><%= project.name %></strong></a>
          <span class="text-muted text-xs d-block"><%= project.slug %></span>
        </td>
        <td data-label="Status"><%- include('../../partials/status-badge', { status: project.status }) %></td>
        <td data-label="Role"><span class="role-pill"><%= role %></span></td>
        <td class="text-muted text-sm" data-label="Created"><%= new Date(project.createdAt).toLocaleDateString('en-GB') %></td>
        <td class="text-right" data-label="Actions">
          <a href="/projects/<%= project.slug %>" class="button button--ghost button--sm">Open</a>
        </td>
      </tr>
      <% }) %>
    </tbody>
  </table>
</div>
<% } %>
